# accounts.hynt.one — the Hynt identity provider. Direct dependencies only.
#
# NOTE ON THE FRAMEWORK: the README describes this service as built on "Cello".
# No such distribution is published, so the HTTP layer here is FastAPI +
# Uvicorn — the same stack the rest of the estate (terminal, intelligence)
# already runs under systemd on this host.

# --- web ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic[email]==2.10.4
pydantic-settings==2.7.0
python-multipart==0.0.20
itsdangerous==2.2.0

# --- database ---
# asyncpg serves requests; psycopg2-binary is only for Alembic's sync engine.
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
psycopg2-binary==2.9.10
alembic==1.14.0

# --- crypto ---
# argon2id for passwords (memory-hard; bcrypt's 72-byte truncation is a
# footgun we do not need). cryptography owns the RS256 signing keys.
argon2-cffi==23.1.0
cryptography==44.0.0
pyjwt[crypto]==2.10.1

# --- misc ---
httpx==0.28.1
qrcode==8.0
pyotp==2.9.0

# --- tests ---
pytest==8.3.4
pytest-asyncio==0.25.0
